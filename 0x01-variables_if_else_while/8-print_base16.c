#include <stdio.h>
#include <stdlib.h>
#include <time.h>

/**
 * main - Entry point
 * Return: Always 0 (Success)
*/
int main(void)
{
	long int n;
	char m;

	for (n = '0'; n <= '9'; n++)
	{
		putchar('n');
	}
	for (m = 'a'; n <= 'f'; m++)
	{
		putchar(n);
	}
		putchar('\n');

	return (0);
}
